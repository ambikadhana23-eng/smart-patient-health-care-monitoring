services:
  - type: web
    name: mediqueue
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_VERSION
        value: 20.0.0
    healthCheckPath: /api/health
    autoDeploy: true
